// 基本:
// - 图类型:      有向图, 无向图
// - 点的标识类型: 自然数, 字符串
// - 边的标识类型: 自然数, 字符串
// - 是否有环
// - 任意两点之间是否允许有重复标识的边
// - 从一个点出发的边中是否允许有重复标识的边
// - 关系存储方式:    链表, 数组

// 时间优化:
// - 遍历能力: 单向遍历, 双向遍历 (只针对有向图)


var u = {
	addEdge: function () {

	},

	removeEdge: function () {

	},

	removeNode: function () {

	},

	hasEdge: function () {

	}
}